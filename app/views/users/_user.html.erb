	
	
	<tr>
     	<td>
      		<%= link_to user.name, user %>
  		</td>

  		<td>
      		<%= user.email %>
  		</td>

  		<td>
	  	<% if current_user.admin? && current_user != user %>
			 <%= link_to "delete", user, method: :delete, confirm: "You sure?", :class => "btn btn-danger"%>
	 	 <% end %>
	 	</td>


	</tr>
